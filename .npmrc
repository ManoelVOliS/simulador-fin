# xlsx (SheetJS) tem uma vulnerabilidade alta conhecida no registro do npm, sem fix disponível
# (prototype pollution + ReDoS no *parser*). Aceita conscientemente: só usamos escrita
# (aoa_to_sheet/writeFile), nunca fazemos parse de arquivo de terceiro — o caminho vulnerável
# nunca é exercitado. Ver TASKS/BACKLOG.md. Desligado aqui pra não quebrar CI se algum dia
# um passo de `npm audit` for adicionado ao workflow.
audit=false
